# FortuinNdivhuwo.py-
A python code that make a list of things you want to do later
#create functions to add and remove task for the list
#In the done_task function, we make a while loop so the user enter only the number of the task they finished
#Create a main while loop that will contain our entire code.
#First with focus on displaying the things to interact with the user,by displaying the list with nothing(no task) and we create options for adding a task and removing it, these options have a number for position reference.
#Ask the user to input the which options they choose by number of the option e.g 1. add task (user enters 1).
#Almost forgot,the input we are asking the user is in a try-except function because the user my not enter the number so we tell them they entered an invalid input unless they input whats required.
#From that input which we placed in a variable(the one we were just talking about) we check if it's 1 or 2 by using if statements which will direct us to know which function (from the ones we created) we should use.
#There's also a third option that lets the user leave the while loop so we also check for that in the if statements
